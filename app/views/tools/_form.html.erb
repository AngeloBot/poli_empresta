<%#= form_with(model: tool, local: true) do |form| %>
<%= form_for([@student,tool]) do |form| %>
  <% if tool.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tool.errors.count, "error") %> prohibited this tool from being saved:</h2>

      <ul>
      <% tool.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :tool_name %>
  </div>

  <div class="field">
    <%= form.label :year %>
    <%= form.number_field :year, id: :tool_year %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :tool_description %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :tool_price %>
  </div>

  <div class="field">
    <%#= form.label :student_id %>
    <%#= form.number_field :student_id, id: :tool_student_id %>
    <%#= form.select :student_id, Student.all.order(:family_name, :given_name).collect{|s|[(s.given_name+" "+s.family_name), s.id]} %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
